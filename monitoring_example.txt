// // Globaler Zustand, der den aktuell ausgewählten Tab speichert
// int current_tab_index = 0;

// // --- Komponente für die Tab-Controller-Leiste ---
// Component TabController(const std::vector<std::string>& tab_names) {
//     // Erstellt einen Container, der Buttons für jeden Tab enthält
//     auto tab_buttons_container = Container::Horizontal({});

//     // Erstellt einen Button für jeden Tab-Namen
//     for (int i = 0; i < tab_names.size(); ++i) {
//         // Fügt einen Button hinzu, der bei Klick den Index ändert
//         tab_buttons_container->Add(
//             Button(tab_names[i], [&] { current_tab_index = i; })
//             // Stilisiert den aktiven Tab anders
//             | (i == current_tab_index ? inverted : nothing) 
//         );
//     }

//     return tab_buttons_container;
// }

// // ... (Code für TabController bleibt gleich) ...

// // --- Hauptfunktion zur Erstellung der Gesamt-UI ---
// ftxui::Component ObserverMonitorComponent() {
//     // 1. Definition der Unterseiten
//     auto cpu_page = Renderer([] {
//         return vbox({
//             text("CPU Monitoring (Aktiver Tab)"),
//             separator(),
//             text("Zeige Echtzeit-CPU-Last und -Temperatur...")
//         }) | border;
//     });

//     auto storage_page = Renderer([] {
//         return vbox({
//             text("Storage Monitoring"),
//             separator(),
//             text("Zeige Festplattenauslastung (lsblk -f Output)...")
//         }) | border;
//     });
    
//     // HIER WAR DER FEHLER: Die innere, geschachtelte Renderer-Klammer wurde entfernt.
//     auto memory_page = Renderer([] {
//         return vbox({ // <--- Korrektur: Die Lambda-Funktion gibt nun direkt ein Element zurück.
//             text("Memory Monitoring"),
//             separator(),
//             text("Zeige RAM-Auslastung...")
//         }) | border;
//     });

//     // 2. Tab-Container: Enthält alle Seiten und steuert, welche sichtbar ist
//     auto tab_container = Container::Tab({
//         cpu_page,
//         storage_page,
//         memory_page
//     }, &current_tab_index); 

//     // 3. Tab-Controller: Die visuelle Leiste oben
//     auto tab_controller = TabController({
//         " [1] CPU-Monitoring ",
//         " [2] Storage-Monitoring ",
//         " [3] Memory-Monitoring "
//     });

//     // 4. Haupt-Layout: Controller über dem Tab-Container
//     auto main_container = Container::Vertical({
//         tab_controller,
//         tab_container
//     });

//     // Renderer, der das Haupt-Layout anzeigt
//     return Renderer(main_container, [=] {
//         return vbox({
//             text("OBSERVER SYSTEM MONITOR") | bold | hcenter,
//             separator(),
//             tab_controller->Render(), 
//             tab_container->Render()  
//         });
//     });
// }
// // ... (main Funktion bleibt gleich) ...

  // auto screen = ScreenInteractive::TerminalOutput();
  // screen.Loop(observer::monitoring::ObserverMonitorComponent());